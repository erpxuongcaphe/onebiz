-- OneBiz ERP - POS permission refinements

-- Ensure Admin has pos.* (already), ensure Cashier exists (024) and can adjust only via POS RPC.
-- No changes required in DB policies here; reserved for future.
