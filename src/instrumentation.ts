/**
 * Reserved for future local instrumentation. External error reporting is
 * intentionally disabled so requests never send application events to Sentry.
 */
export function register() {
  // No-op by design.
}
