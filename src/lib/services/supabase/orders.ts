/**
 * Supabase service: Sales Orders (Đơn hàng)
 *
 * No dedicated sales_orders table in DB yet.
 * Re-export from mock services until schema is extended.
 */

export { getOrders, getOrderStatuses } from "../mock/orders";
